<Page
    x:Class="UlmUniversityNews.Views.StartPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:UlmUniversityNews.Views"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d"
    Background="{ThemeResource ApplicationPageBackgroundThemeBrush}">
    <Page.Resources>
        <Flyout x:Key="ErrorFlyout">
            <Grid Background="Black">
                <TextBlock 
                    FontSize="14"
                    Foreground="White"
                    TextWrapping="Wrap" 
                    x:Name="ErrorFlyoutMessage"
                    x:Uid="ErrorFyloutMessageKey"
                    Text="" />
            </Grid>
        </Flyout>
    </Page.Resources>
    
    <!-- Command Bar -->
    <Page.BottomAppBar>
        <CommandBar>
            <AppBarButton x:ConnectionId='1' 
                x:Name="CreateAccount"
                Icon="Accept"
                Label="create account"
                                           />
        </CommandBar>
    </Page.BottomAppBar>

    <Grid x:Name="LayoutRoot">

        <Grid.ChildrenTransitions>
            <TransitionCollection>
                <EntranceThemeTransition/>
            </TransitionCollection>
        </Grid.ChildrenTransitions>

        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <!-- Titelbereich -->
        <Grid Grid.Row="0" Margin="0,0,0,0" Height="60">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="Auto" />
                <ColumnDefinition Width="*" />
            </Grid.ColumnDefinitions>

            <!-- Drawer Icon - zeigt hier das Uni Logo -->
            <Image Margin="5" 
                   x:Name="DrawerIcon" 
                   Grid.Column="0"
                   Source="/Assets/AppIcons/Logo-Uni.png"
                   HorizontalAlignment="Left" />

            <!-- Drawer Text -->
            <TextBlock Margin="10,0,0,0"
                       Grid.Column="1"
                       x:Uid="StartPageTitle"
                       Text="Start Page"
                       Foreground="White"
                       VerticalAlignment="Center"
                       FontSize="18" />
        </Grid>

        <!-- Inhalte innerhalb des folgenden Rasters platziert. -->
        <Grid Grid.Row="1" x:Name="ContentRoot" Margin="19,9.5,19,0">
            <StackPanel>
                <TextBlock Text="Ulm University News" 
                           Style="{StaticResource SubheaderTextBlockStyle}" />
                <TextBlock Margin="0,10,0,0" 
                           Text="Willkommen bei University News." 
                           FontSize="16" />
                <TextBlock Text="Um diese Anwendung zu nutzen, müssen sie einen Nutzernamen angeben. Der Nutzername wird verwendet, um von Ihnen erstellte Nachrichten für andere Nutzer identifzierbar zu machen." 
                           TextWrapping="Wrap"
                           FontSize="16" />
                <TextBlock Margin="0,10,0,0"
                           Text="Bitte geben Sie einen Nutzernamen an:"
                           FontSize="16" />
                <TextBox x:Name="UserName"
                         Header="Nutzername"
                         InputScope="AlphanumericFullWidth"
                         FlyoutBase.AttachedFlyout="{StaticResource ErrorFlyout}"/>
                <TextBlock x:Name="ErrorText" 
                           FontSize="14" 
                           Foreground="Red" 
                           TextWrapping="Wrap" />
            </StackPanel>
        </Grid>
    </Grid>
</Page>

