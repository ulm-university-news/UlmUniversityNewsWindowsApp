<Page
    x:Class="UlmUniversityNews.Views.Homescreen.Homescreen"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:UlmUniversityNews.Views.Homescreen"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:drawerLayout="using:DrawerLayout"
    mc:Ignorable="d"
    Background="{ThemeResource ApplicationPageBackgroundThemeBrush}">
    <!-- CommandBar - Die CommandBar wird per Code initialisiert. -->
    <Page.BottomAppBar>
        <CommandBar x:Name="HomescreenCommandBar">
        </CommandBar>
    </Page.BottomAppBar>
    
    
    <!--Definition der seitenspezifischen Ressourcen.-->
    <!--
    <Page.Resources>
        <ContentDialog x:Key="lockScreenAccessDeniedContentDialog"
                       x:Name="lockScreenAccessDeniedCD"
                       PrimaryButtonClick="lockScreenAccessDeniedCD_PrimaryButtonClick"
                       Opened="lockScreenAccessDeniedCD_Opened">
            <StackPanel>
                <TextBlock x:Uid="lockScreenAccessDeniedMessage" 
                           x:Name="lockScreenAccessDeniedMessage"
                           TextWrapping="WrapWholeWords"
                           FontSize="16"
                           Foreground="White">
                    Es ist der Anwendung aktuell nicht erlaubt Hintergrundaufgaben auszuführen. 
                    Bitte beachten Sie, dass die Aktualisierungen von Inhalten per Push Nachrichten somit nicht funktioniert.
                    Sie müssen die Inhalte manuell aktualisieren. Sie können der Anwendung die Ausführung von Hintergrundaufgaben erlauben unter Einstellungen-Stromsparmodus-Nutzung.
                </TextBlock>
                <CheckBox x:Uid="doNotShowAccessDeniedWarningAgainCheckBox"
                          x:Name="doNotShowAccessDeniedWarningAgainCheckBox">
                    Nicht mehr anzeigen
                </CheckBox>
            </StackPanel>
        </ContentDialog>
    </Page.Resources>
        -->

    
    <Grid x:Name="LayoutRoot">
        <Grid.ChildrenTransitions>
            <TransitionCollection>
                <EntranceThemeTransition/>
            </TransitionCollection>
        </Grid.ChildrenTransitions>

        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <!-- Titelbereich - Drawer -->
        <Grid Grid.Row="0" Margin="0,0,0,0" Height="60">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="Auto" />
                <ColumnDefinition Width="*" />
            </Grid.ColumnDefinitions>
            
            <!-- Drawer Icon -->
            <Image x:ConnectionId='1' Margin="5" 
                   x:Name="DrawerIcon" 
                   Grid.Column="0"
                   Source="/Assets/Drawer/drawer_icon.png"
                   HorizontalAlignment="Left"
                                              />
            
            <!-- Drawer Text -->
            <TextBlock Grid.Column="1"
                       x:Uid="HomescreenTitle"
                       Text="Homescreen"
                       Foreground="White"
                       VerticalAlignment="Center"
                       FontSize="18" />
        </Grid>

        <!--Inhalt der Seite und Inhalt des Drawer Menü-->
        <drawerLayout:DrawerLayout Grid.Row="1" x:Name="DrawerLayout">
            
            <!-- Inhalt der Seite -->
            <Grid x:Name="MainFragment" Background="{StaticResource PhoneBackgroundBrush}">
                <Pivot x:Uid="HomescreenPivot" x:Name="HomescreenPivot">
                    <!-- Pivotelement 1-->
                    <PivotItem x:Uid="MyChannels"
                           Margin="19,14.5,0,0"
                           Header="Meine Kanäle">
                        <!-- TODO -->
                        <TextBlock x:Name="Test"
                                   FontSize="25"
                                   Text="Jo Servus, das ist ein Test Text. Den schreib ich extra groß, damit man ihn auch sieht."
                                   TextWrapping="Wrap"/>
                    </PivotItem>
                    <!-- Pivotelement 2-->
                    <PivotItem x:Uid="MyGroups"
                           Margin="19,14.5,0,0"
                           Header="Meine Gruppen">
                        <!-- TODO -->
                    </PivotItem>
                    <!-- Pivotelement 3-->
                    <PivotItem x:Uid="ApplicationSettings"
                           Margin="19,14.5,0,0"
                           Header="Anwendungseinstellungen">
                        <!-- TODO -->
                    </PivotItem>
                </Pivot>
            </Grid>
            
            <!-- Inhalt des Drawer Menüs-->
            <Grid x:Name="ListFragment" Background="#F4F4F4">
                <ListView x:Name="ListMenuItems">
                    <ListView.ItemTemplate>
                        <DataTemplate>
                            <TextBlock Text="{Binding}"
                                       Margin="10"
                                       HorizontalAlignment="Left"
                                       VerticalAlignment="Center"
                                       FontSize="18"
                                       Foreground="Black" />
                        </DataTemplate>
                    </ListView.ItemTemplate>
                </ListView>
            </Grid>

        </drawerLayout:DrawerLayout>
    </Grid>
</Page>

